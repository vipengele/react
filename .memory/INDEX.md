# Memory index

<!-- DO NOT EDIT — generated by `agtk memory index` from note frontmatter. -->

11 notes. Read one with `agtk memory show <name>`.

## biome-config-non-default-choices  (rationale, verified)
Biome is the only formatter/linter (ADR 0010); the **/*.svg exclusion exists because noSvgWithoutTitle fails brand assets, and Markdown is formatted by nothing.

- biome.json
- package.json
- docs/adr/0010-biome-owns-formatting.md

## bundle-check-floating-ui-markers-lack-positive-control  (gotcha, suspect)
bundle-check only asserts floating-ui markers are absent; nothing committed proves they appear when floating-ui is bundled.

- packages/ui/bundle-check/run.mjs

## custom-property-read-back-is-unresolved  (gotcha, verified)
getPropertyValue on an unregistered --tandiko-* property returns its calc() token stream, not pixels; consume it as a real property (and in a browser test) to measure it.

- packages/ui/src/theme-scalars.browser.test.ts
- packages/tokens/src/theme.ts
- packages/ui/vitest.config.ts

## element-box-cannot-detect-own-padding  (gotcha, verified)
Comparing getBoundingClientRect().left of two elements passes regardless of their own inline padding; measure text position with a Range over the text instead.

- packages/ui/src/FormField/FormField.browser.test.tsx

## field-shell-control-must-be-direct-child  (gotcha, verified)
FieldShell's focus/open/invalid/disabled rules match only a direct child, so the control must not be wrapped, and nothing inside the shell may dim itself.

- packages/ui/src/FieldShell/FieldShell.stylesheet.ts
- packages/ui/src/FieldShell/FieldShell.test.tsx
- packages/ui/src/PasswordInput/PasswordInput.stylesheet.ts
- packages/ui/src/PasswordInput/PasswordInput.tsx

## fieldset-layout-is-shaped-by-the-legend  (rationale, verified)
FieldSet is a block box with a padding-0 legend and sibling-combinator margins because a native legend straddles the border; the margin rule also assumes React hoists its <style>.

- packages/ui/src/FieldSet/FieldSet.stylesheet.ts
- packages/ui/src/FieldSet/FieldSet.tsx
- packages/ui/src/FieldSet/FieldSet.test.tsx
- packages/ui/src/FormField/FormField.browser.test.tsx

## root-agent-instructions-are-gitignored  (gotcha, verified)
Only the root CLAUDE.md/AGENTS.md are git-ignored, so re-rendering them produces no diff; the per-package ones are tracked.

- .gitignore
- agentic/tandiko-repo.md

## test-task-depends-on-own-build  (rationale, verified)
turbo's test task must depend on the package's own build, because bundle-check tests read dist/.

- turbo.json
- packages/ui/bundle-check/entry.js
- packages/icons/bundle-check/entry.js

## tests-read-source-via-import-meta-glob-not-node-fs  (gotcha, verified)
No workspace depends on @types/node, so a test that reads files must use Vite's import.meta.glob ?raw, not node:fs.

- package.json
- packages/*/package.json
- apps/*/package.json
- tsconfig.base.json
- packages/ui/src/no-fallback-var-reads.test.ts

## ui-token-reads-carry-no-fallback  (invariant, verified)
No var(--tandiko-*) read in packages/ui/src has a literal fallback; a glob-driven test enforces it, including in components that do not exist yet.

- packages/ui/src/*/*.ts
- packages/ui/src/*/*.tsx
- packages/ui/src/no-fallback-var-reads.test.ts
- packages/tokens/src/theme.ts
- packages/tokens/src/base-stylesheet.ts
- docs/adr/0009-components-read-role-tokens-with-no-literal-fallback.md

## vitest-browser-cdp-import-specifier  (gotcha, verified)
Browser tests emulate media features via cdp() imported from "vitest/browser"; the documented "@vitest/browser/context" does not resolve from packages/ui.

- packages/ui/src/theme-motion.browser.test.ts
- packages/ui/package.json
- packages/ui/vitest.config.ts
